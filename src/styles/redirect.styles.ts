import { SxProps } from '@mui/material';

export const redirectContainerStyles: SxProps = {
    display: 'flex',
    justifyContent: 'center',
    w: '100%',
};

export const redirectWrapperStyles: SxProps = {
    alignItems: 'center',
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    gap: 2,
    maxWidth: '500px',
    my: 5,
    w: '100%',
    textAlign: 'center',
};

export const redirectBoldStyles: SxProps = {
    fontWeight: 600,
};
