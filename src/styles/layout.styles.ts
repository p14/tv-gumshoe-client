import { SxProps } from '@mui/material';

export const layoutContainerStyles: SxProps = {
    display: 'flex',
    flexDirection: 'column',
    minHeight: '100dvh',
};

export const layoutContentContainerStyles: SxProps = {
    flexGrow: 1,
    width: '100%',
};
