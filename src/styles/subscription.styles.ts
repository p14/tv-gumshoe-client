import { SxProps } from '@mui/material';

export const userSubscriptionContainerStyles: SxProps = {
    display: 'flex',
    flexWrap: 'wrap',
    justifyContent: 'center',
    height: '100%',
};

export const userSubscriptionTitleStyles: SxProps = {
    textAlign: 'center',
};
